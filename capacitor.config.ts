import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.jondi',
  appName: 'jondi',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
